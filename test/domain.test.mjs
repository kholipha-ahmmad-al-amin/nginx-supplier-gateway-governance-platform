import{expect,test}from'vitest';import{gateways,GatewayError}from'../domain.mjs';const bad=(c,f)=>{try{f();return false}catch(e){return e instanceof GatewayError&&e.code===c}};test('governs routes',()=>{const d=gateways();d.define('gateway-engineer','e',{id:'NGX-328',supplier:'Crown Supply',path:'/supplier-risk'});expect(bad('conflict',()=>d.activate('gateway-operator','o','NGX-328'))).toBe(true);d.review('security-governor','g','NGX-328','Security review validates supplier gateway route controls.');expect(d.activate('gateway-operator','o','NGX-328').state).toBe('active');expect(bad('forbidden',()=>d.review('gateway-engineer','e','NGX-328','Security review validates supplier gateway route controls.'))).toBe(true);expect(bad('not_found',()=>d.review('security-governor','g','NGX-X','Security review validates supplier gateway route controls.'))).toBe(true)});
